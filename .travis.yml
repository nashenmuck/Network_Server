sudo: required

language: generic

services:
  - docker
script:
  - docker build -t nashenmuck/network:testing -t nashenmuck/network:testing-$(echo $TRAVIS_COMMIT | cut -c1-7) .
deploy:
  provider: script
  script: docker login -u $DOCKER_USER -p $DOCKER_PASSWORD && docker push nashenmuck/network:testing && docker push nashenmuck/network:testing-$(echo $TRAVIS_COMMIT | cut -c1-7)
  on:
    branch: testing
